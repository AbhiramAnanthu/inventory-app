<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Category View</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 text-gray-900">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4">Categories</h1>
        <table class="min-w-full bg-white shadow-md rounded-lg overflow-hidden">
            <thead class="bg-gray-800 text-white">
                <tr>
                    <th class="py-2 px-4">Name</th>
                    <th class="py-2 px-4">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(function(category) { %>
                    <tr class="border-b">
                        <td class="py-2 px-4 text-center">
                            <%= category.name %>
                        </td>
                        <td class="py-2 px-4">
                            <div class="flex space-x-2 justify-center align-center flex-row">
                                <a href="/category/view-items/" class="text-blue-500 hover:underline">View</a>

                                <a href="/category/get-update-form?name=<%= category.name %>"
                                    class="text-yellow-500 hover:underline">Update</a>

                                <form action="/category/<%= category.name %>/category-del" method="POST" class="inline">
                                    <button type="submit" class="text-red-500 hover:underline">Delete</button>
                                </form>
                            </div>
                        </td>

                    </tr>
                    <% }); %>
            </tbody>
        </table>
        <a href="/category/category-new"
            class="mt-4 inline-block bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-600">Create Category</a>
    </div>
</body>

</html>